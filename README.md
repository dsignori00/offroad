# offroad
All the MatLab utils needed for a remarkable offroad navigation data analysis!

## Repository content
- `offroad/`: scripts migrated from the `offroad` folder of `dsignori00/matlab`
- `common/`: shared dependencies required by `offroad` scripts (constants, utils, and plotting/graphic helpers)
